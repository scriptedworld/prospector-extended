# Task 1.07 - Align .prospector.yaml with Template

## Status: [ ] Pending

## Overview

Update .prospector.yaml to match python-base-template standards while
preserving prospector-extended-specific configuration (this project IS the
quality tool, so some settings are self-referential).

## Task Type: config

## Files

**Add:** (none)
**Modify:** `.prospector.yaml`
**Delete:** (none)
**Context:** `../python-base-template/template/.prospector.yaml`, current `.prospector.yaml`

## Requirements

1. Add `whitelist-paths` to vulture config (reference `vulture_whitelist.py`)
2. Remove `max-line-length: 120` (should be 150, managed by ruff via pyproject.toml)
3. Remove duplicate tool options that are already in pyproject.toml (mypy options duplicated)
4. Simplify mypy section to reference `config-file: pyproject.toml`
5. Simplify interrogate section to reference pyproject.toml config
6. Add `ignore-private: true` to interrogate options (template has it)
7. Add `ignore-property-decorators: true` to interrogate options (template has it)
8. Remove `test-warnings: false` (template doesn't have it, tests already excluded)
9. Ensure disabled tools section matches template pattern

## Test Cases

| Req | Type | Description |
|-----|------|-------------|
| 1 | Positive | `poe quality` runs successfully with updated config |
| 2-9 | Positive | No new quality messages introduced by config changes |

---

## Checklist (Config Task)

- [ ] Pre-change verification: `poe checks` baseline captured
- [ ] Implementation: .prospector.yaml updated
- [ ] Post-change verification: `poe checks` still passes
- [ ] Pre/post comparison: no new messages introduced

---

## Evidence

### Pre-Change Baseline
(capture before starting)

### Post-Change Verification
(capture after completing)

---

## Attempts

(none yet)
