# Task 1.03 - Overhaul pyproject.toml

## Status: [ ] Pending

## Overview

Align pyproject.toml with python-base-template standards. This is the largest
infrastructure task, covering build system, dependency management, poe tasks,
pytest configuration, coverage, commitizen, and ruff rules.

## Task Type: config

## Files

**Add:** (none)
**Modify:** `pyproject.toml`
**Delete:** (none)
**Context:** `../python-base-template/template/pyproject.toml.jinja`

## Requirements

### Build System
1. Switch build-backend from `hatchling` to `uv_build>=0.9.11,<0.10.0`
2. Add `[tool.uv]` section with `python-preference = "only-system"`

### Dependencies
3. Convert `[project.optional-dependencies] dev` to `[dependency-groups] dev`
4. Update dev dependency versions to match template minimums
5. Add missing dev deps: `commitizen`, `pdoc`, `pre-commit`, `pytest-json-report` (bandit, pylint, vulture, ruff are already runtime via prospector extras — do NOT duplicate)
6. Remove `[tool.hatch.build.targets.wheel]` and `[tool.hatch.build.targets.sdist]` sections (uv_build auto-detects src layout)

### Pytest Configuration
7. Add `--strict-markers` and `--strict-config` to addopts
8. Add branch coverage: `--cov=src`, `--cov-branch`, `--cov-report=term-missing`
9. Add JSON/XML reports: `--cov-report=json:.task/coverage.json`, `--cov-report=xml:.task/coverage.xml`, `--json-report`, `--json-report-file=.task/test_results.json`, `--junitxml=.task/test_results.xml`
10. Add `markers` for slow tests

### Coverage Configuration
11. Add `[tool.coverage.run]` with `branch = true`, `source = ["src"]`
12. Add `[tool.coverage.report]` with `fail_under = 80`, `show_missing = true`, `exclude_lines`

### Poe Tasks
13. Add `sync-setup` composite task (uv sync + vendor install)
14. Add `ensure-hooks` composite task (pre-commit install x3)
15. Add `format-check` task
16. Restructure `test` to `pytest --no-cov -m 'not slow'`
17. Add `test-cov`, `test-slow`, `test-all` tasks
18. Add `validate-coverage` task using `.claude/scripts/validate_coverage.py`
19. Make `test-coverage` composite: `["test-cov", "validate-coverage"]`
20. Add standalone `types` task (`mypy src/`)
21. Add standalone `security` task (`bandit -r src/ -f json -o .task/bandit.json || true`)
22. Add `xray` task (`xray-audit`)
23. Add `quality` task (prospector-extended with tee to .task/)
24. Restructure `checks` to composite: `["test-coverage", "format-check", "quality"]`
25. Add `docs` task (`pdoc --output-dir docs/api src/prospector_extended`)
26. Add `clean` task

### Ruff Rules
27. Add missing rule sets from template: `N`, `T20`, `RET`, `PTH`, `ERA`, `RUF` (keep existing `ARG`)
28. Add `vulture_whitelist.py` per-file-ignores (`B018`)
29. Add `docstring-code-line-length = 100` to format config
30. Keep `D107` in ignore list (interrogate handles __init__ docstrings; see design doc)

### Commitizen
31. Add `[tool.commitizen]` section with conventional commits config

### Other
32. Add `files = ["src"]` and `exclude = ["tests", "__pycache__", ".venv"]` to mypy config
33. Add `targets = ["src"]` to bandit config
34. Update homepage URL to actual project repository

## Test Cases

| Req | Type | Description |
|-----|------|-------------|
| 1-2 | Positive | `uv sync` succeeds with uv_build backend |
| 3-6 | Positive | `uv sync` installs all dev dependencies |
| 7-10 | Positive | `uv run poe test` runs with correct flags |
| 11-12 | Positive | Coverage config applied correctly |
| 13-26 | Positive | Each poe task runs without error |
| 24 | Positive | `poe checks` runs composite (test-coverage + format-check + quality) |
| 27-30 | Positive | `poe lint` applies all rule sets |
| 31 | Positive | `uv run cz check --message "feat: test"` passes |

---

## Checklist (Config Task)

- [ ] Pre-change verification: `poe checks` baseline captured
- [ ] Implementation: pyproject.toml updated
- [ ] Post-change verification: `uv sync` + `poe checks` pass
- [ ] Pre/post comparison: no new failures introduced
- [ ] All new poe tasks verified individually

---

## Evidence

### Pre-Change Baseline
(capture `poe checks` output before starting)

### Post-Change Verification
(capture `poe checks` output after completing)

---

## Attempts

(none yet)
