# Task 1.05 - Add Pre-commit Configuration

## Status: [ ] Pending

## Overview

Add .pre-commit-config.yaml matching the python-base-template standard.
Provides automated quality gates at commit and push time.

## Task Type: config

## Files

**Add:** `.pre-commit-config.yaml`
**Modify:** (none)
**Delete:** (none)
**Context:** `../python-base-template/template/.pre-commit-config.yaml`

## Requirements

1. Create `.pre-commit-config.yaml` from template with 4 repo sections:
   - `pre-commit-hooks` (trailing whitespace, end-of-file, YAML/TOML/JSON, large files)
   - `ruff-pre-commit` (ruff lint --fix, ruff format)
   - `commitizen` (commit-msg validation)
   - `local` xray hook (pre-push vulnerability scan)
2. Hook versions should match or exceed template versions
3. `poe ensure-hooks` (from task 1.03) should successfully install all hooks

## Test Cases

| Req | Type | Description |
|-----|------|-------------|
| 1 | Positive | .pre-commit-config.yaml parses without error |
| 2 | Positive | `pre-commit install` succeeds |
| 3 | Positive | `pre-commit run --all-files` runs configured hooks |

---

## Checklist (Config Task)

- [ ] Pre-change verification: baseline captured
- [ ] Implementation: .pre-commit-config.yaml created
- [ ] Post-change verification: hooks install and run
- [ ] Pre/post comparison: no regressions

---

## Evidence

### Pre-Change Baseline
(capture before starting)

### Post-Change Verification
(capture after completing)

---

## Attempts

(none yet)
