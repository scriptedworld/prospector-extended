# Task 3.03 - Add Tests for cli.py

## Status: [ ] Pending

## Overview

Add test coverage for the CLI module (cli.py, 57 LOC). This is the public
API entry point with `patch_prospector_tools()` and `main()` currently
at zero coverage.

## Task Type: tdd

## Files

**Add:** `tests/test_cli.py`
**Modify:** (none)
**Delete:** (none)
**Context:** `src/prospector_extended/cli.py`, `src/prospector_extended/__init__.py`

## Requirements

1. Test `patch_prospector_tools()` registers all expected tools
2. Test `patch_prospector_tools()` replaces existing tool entries (not just adds)
3. Test `main()` calls prospector's main function
4. Test that tool registration is idempotent (calling twice doesn't break)
5. Test that registered tools are the correct class types

## Test Cases

| Req | Type | Description |
|-----|------|-------------|
| 1 | Positive | After patching, TOOLS dict contains mypy, complexipy, interrogate, vulture |
| 2 | Positive | After patching, TOOLS["mypy"] is MypyTool (not prospector's default) |
| 3 | Positive | main() invokes prospector execution |
| 4 | Positive | Calling patch_prospector_tools() twice doesn't raise |
| 5 | Positive | Each registered tool is correct subclass |

---

## Checklist (TDD Task)

### RED Phase
- [ ] Tests written for cli.py
- [ ] Tests fail with AssertionError (not ImportError/SyntaxError)
- [ ] All quality gates pass (lint, types)
- [ ] Tests committed

### GREEN Phase
- [ ] Tests pass (cli.py may already satisfy them)
- [ ] ALL quality gates pass
- [ ] Coverage meets threshold
- [ ] Implementation committed (if changes needed)

### REFACTOR Phase
- [ ] Code cleaned up
- [ ] Tests still pass
- [ ] No new functionality added

---

## Evidence

### RED Phase
(test output showing failures)

### GREEN Phase
(test output showing passes)

### Quality Gate
(poe checks output)

---

## Attempts

(none yet)
