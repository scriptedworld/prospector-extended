# Task 3.09 - Add Error Path and Edge Case Tests

## Status: [ ] Pending

## Overview

Add tests for error paths and edge cases across all tools, addressing
the code review finding that tool tests verify success but not failure
conditions.

## Task Type: tdd

## Files

**Add:** (none)
**Modify:**
- `tests/test_complexipy_tool.py`
- `tests/test_interrogate_tool.py`
- `tests/test_mypy_tool.py`
- `tests/test_parsing.py`

**Delete:** (none)
**Context:** `src/prospector_extended/tools/`, `docs/CODE_REVIEW.md` (findings)

## Requirements

1. Test tool behavior with nonexistent file paths (should return empty, not crash)
2. Test tool behavior with files that cause parsing errors
3. Test mypy output parsing with malformed JSON (text fallback)
4. Test mypy output parsing when both JSON and text errors exist
5. Test fixture file existence before assertions (prevent vacuously passing tests)
6. Test matcher priority ordering (higher priority matcher wins)
7. Test schema fingerprint changes when schema changes

## Test Cases

| Req | Type | Description |
|-----|------|-------------|
| 1 | Negative | Nonexistent file path returns empty message list |
| 2 | Negative | File with syntax error is handled gracefully |
| 3 | Negative | Malformed JSON falls back to text parsing |
| 4 | Edge | Mixed JSON/text output parsed correctly |
| 5 | Positive | Fixture files verified to exist before test runs |
| 6 | Positive | TypeTagMatcher (priority 100) beats RequiredFieldsMatcher (priority 50) |
| 7 | Positive | Fingerprint changes when schema definition changes |

---

## Checklist (TDD Task)

### RED Phase
- [ ] Tests written for error paths
- [ ] Tests fail with AssertionError
- [ ] All quality gates pass (lint, types)
- [ ] Tests committed

### GREEN Phase
- [ ] ALL tests pass
- [ ] ALL quality gates pass
- [ ] Coverage meets threshold
- [ ] Implementation committed (if code changes needed)

### REFACTOR Phase
- [ ] Code cleaned up
- [ ] Tests still pass
- [ ] No new functionality added

---

## Evidence

### RED Phase
(test output showing failures)

### GREEN Phase
(test output showing passes)

### Quality Gate
(poe checks output)

---

## Attempts

(none yet)
