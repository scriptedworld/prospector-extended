# Task 3.07 - Add Tests for ExtendedToolBase

## Status: [ ] Pending

## Overview

Add test coverage for ExtendedToolBase (base.py, 133 LOC). The base class
provides template method workflow, message creation, and ignore_codes
filtering that is tested indirectly through subclasses but never directly
verified.

## Task Type: tdd

## Files

**Add:** `tests/test_base_tool.py`
**Modify:** (none)
**Delete:** (none)
**Context:** `src/prospector_extended/tools/base.py`, `tests/conftest.py`

## Requirements

1. Test `_create_message()` produces correct Message with all fields
2. Test `_create_message()` with ignore_codes filters suppressed messages
3. Test `_create_message()` returns None when code is in ignore list
4. Test Location object creation from filepath and line number
5. Test run() iterates over files and calls _analyze_file() for each
6. Test run() aggregates messages from multiple files

## Test Cases

| Req | Type | Description |
|-----|------|-------------|
| 1 | Positive | _create_message() returns Message with code, file, line, source |
| 2 | Positive | Message with ignored code returns None |
| 3 | Negative | Message with non-ignored code is returned |
| 4 | Positive | Location has correct path, module, function, line, character |
| 5 | Positive | run() calls _analyze_file() for each file |
| 6 | Positive | run() returns combined messages from all files |

---

## Checklist (TDD Task)

### RED Phase
- [ ] Tests written for base.py
- [ ] Tests fail with AssertionError
- [ ] All quality gates pass (lint, types)
- [ ] Tests committed

### GREEN Phase
- [ ] ALL tests pass
- [ ] ALL quality gates pass
- [ ] Coverage meets threshold
- [ ] Implementation committed

### REFACTOR Phase
- [ ] Code cleaned up
- [ ] Tests still pass
- [ ] No new functionality added

---

## Evidence

### RED Phase
(test output showing failures)

### GREEN Phase
(test output showing passes)

### Quality Gate
(poe checks output)

---

## Attempts

(none yet)
